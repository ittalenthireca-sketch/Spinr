# Spinr Upstream Sync Reports

Automated reports generated by `.github/workflows/upstream-sync.yml` (twice daily at 07:00 and 19:00 UTC).

Each report is named `YYYY-MM-DD-upstream-sync.md` and linked from the GitHub Actions job summary.

